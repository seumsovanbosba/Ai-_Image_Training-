# Personal progress tracker

This folder is created on first use of the **Progress** page or:

```bash
PYTHONPATH=. python -c "from gallery_app.progress import load_progress; load_progress()"
```

It is listed in `.gitignore`. Do not commit HourMeng or Bosba's personal ticks. Team evidence lives in `benchmark/` and the report, not here.
